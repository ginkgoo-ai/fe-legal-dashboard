/**
 * virtual list config
 */

/**
 * activity config
 */
export const ActivityVirtualConfig = {
  rowHeight: 110, // 每行高度
  overscanCount: 5, // 预渲染行数
  loadMoreDistance: 200, // 触发加载的距离
  pageSize: 20, // 每页数据量
  loadingRows: 10, // 加载时显示的骨架屏数量
};
